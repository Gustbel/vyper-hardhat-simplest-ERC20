# vyper-hardhat-simplest-ERC20

Deploy script in local
```
npx run scripts/deploy
```

Deploy script in Kovan
```
npx run scripts/deploy --network kovan
```

Deploy script in Rinkeby
```
npx run scripts/deploy --network rinkeby
```